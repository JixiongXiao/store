高阶组件（HOC）是 React 中用于复用组件逻辑的一种高级技巧。High Order Component
HOC 自身不是 React API 的一部分，它是一种基于 React 的组合特性而形成的设计模式。

具体而言，高阶组件是参数为组件，返回值为新组件的函数。

笔记：
简单而言高阶组件就是将组件作为参数，在内将组件强化成新组件的一种函数
其实就是复用了子组件的一些逻辑，父组件作为元件，子组件用来渲染父组件的数据，然后返回一个新的组件

比如react.memo和 react-redux中的connect都是高阶组件。